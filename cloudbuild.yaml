steps:
      # Install
      - name: 'gcr.io/cloud-builders/npm'
        args: ['install']
      # Build
      - name: 'gcr.io/cloud-builders/npm'
        args: ['run', 'build']
        # Deploy
      - name: gcr.io/weather-88364/firebase
        args: ['deploy', '--project=weather-88364', '--only=hosting']
